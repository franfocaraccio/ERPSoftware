# Conexión de la APLICACIÓN — rol sin BYPASSRLS, en session mode (no transaction pooling).
DATABASE_URL=postgresql://erp_app:erp_app@localhost:5432/erp

# Conexión para MIGRACIONES — rol owner del schema. Solo la usa drizzle-kit.
DATABASE_URL_MIGRATIONS=postgresql://erp:erp@localhost:5432/erp
